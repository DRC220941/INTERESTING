from fastapi.testclient import TestClient from src.main import app, working_mem  client = TestClient(app)  # ===== FIXTURE POUR RÉINITIALISER LA MÉMOIRE AVANT CHAQUE TEST ===== import pytest  @pytest.fixture(autouse=True) def reset_memory():     """Réinitialise la mémoire de travail avant chaque test"""     working_mem.reset()     yield     # Optionnel : nettoyage après le test (si nécessaire)     working_mem.reset()  # ===== TESTS ===== def test_health_check():     response = client.get("/health")     assert response.status_code == 200     assert response.json()["status"] == "healthy"  def test_predict_empty():     # La mémoire est déjà réinitialisée par la fixture     response = client.post("/predict", json={"values": []})     assert response.status_code == 200     assert "Données insuffisantes" in response.json()["warnings"]  # <-- Accent ajouté  def test_predict_with_data():     response = client.post("/predict", json={"values": [1.5, 2.0, 1.8]})     assert response.status_code == 200     assert len(response.json()["predictions"]) == 3     assert response.json()["top_features"]["count"] == 3  def test_reset():     # Ajout de données     working_mem.add_multiple([1.5, 2.0])     assert len(working_mem.get_all()) == 2     # Réinitialisation     response = client.post("/reset")     assert response.status_code == 200     assert len(working_mem.get_all()) == 0  def test_history():     working_mem.add_multiple([1.0, 2.0, 3.0])     response = client.get("/history")     assert response.status_code == 200     assert response.json()["count"] == 3