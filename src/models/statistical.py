import numpy as np from typing import List, Tuple  class StatisticalModel:     """Mod�le bas� sur des statistiques simples (moyenne, variance, tendances)"""      def __init__(self, window_size: int = 5):         self.window_size = window_size         self.history: List[float] = []      def update(self, new_values: List[float]):         """Met � jour l'historique avec de nouvelles valeurs"""         self.history.extend(new_values)      def predict(self) -> Tuple[List[float], List[float]]:         """Retourne 3 pr�dictions + confiances"""         if len(self.history) < 3:             return [1.0, 1.5, 2.0], [0.5, 0.4, 0.3]          # Calculs statistiques         mean = np.mean(self.history[-self.window_size:])         std = np.std(self.history[-self.window_size:])          # Pr�dictions : moyenne � �cart-type         pred_low = max(0.1, mean - std)         pred_mid = mean         pred_high = mean + std          # Confiances (bas�es sur la stabilit� des donn�es)         confidence = min(0.95, 0.7 + (0.25 if std < mean * 0.3 else 0))          return [             round(pred_low, 2),             round(pred_mid, 2),             round(pred_high, 2)         ], [confidence, confidence * 1.1, confidence * 0.9]